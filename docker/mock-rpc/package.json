{
  "name": "scoutoff-mock-soroban-rpc",
  "version": "0.1.0",
  "private": true,
  "description": "Local, credential-free mock of the Soroban RPC endpoints lib/contract.ts and lib/stellar.ts talk to. Used only by docker-compose.yml for local development — never a target for real transactions.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "12.1.0"
  }
}
